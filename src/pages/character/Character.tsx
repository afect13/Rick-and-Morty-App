import { CharacterProfile, PageLayout } from '../../components';

export const Character = () => {
  return (
    <PageLayout title={'Character'}>
      <CharacterProfile />
    </PageLayout>
  );
};
